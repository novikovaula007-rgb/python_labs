# ЛР2 — Коллекции и матрицы

## Задание 1 - Массивы (arrays.py)
Реализует функции для работы со списками: поиск минимума и максимума (`min_max`), получение отсортированного списка уникальных значений (`unique_sorted`) и "расплющивание" двумерной структуры в одномерный список (`flatten`). Переиспользуемая логика вынесена в `src/lib/arrays.py`.

![Задание 1](../images/lab02/ex_01.png)

## Задание 2 - Матрицы (matrix.py)
Реализует операции над прямоугольными матрицами: транспонирование (`transpose`), расчет сумм элементов по строкам (`row_sums`) и по столбцам (`col_sums`). Содержит проверки на «рваные» матрицы с выбросом `ValueError`. Переиспользуемая логика вынесена в `src/lib/matrix.py`.

![Задание 2](../images/lab02/ex_02.png)

## Задание 3 - Кортежи и форматирование (tuples.py)
Обрабатывает кортежи с данными студентов вида `(fio, group, gpa)` и форматирует их в итоговую строку. Включает очистку лишних пробелов, генерацию инициалов, валидацию типов и округление GPA до двух знаков. Переиспользуемая логика вынесена в `src/lib/tuples.py`.

![Задание 3](../images/lab02/ex_03.png)